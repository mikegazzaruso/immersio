Create a Three.js + WebXR game from scratch based on the following concept: $ARGUMENTS

The arguments may include an optional path to a folder of .glb 3D model files as the last token (e.g., `/dream a forest adventure ~/my-models`). Parse according to the dream skill instructions.

Read `CLAUDE.md` first, then follow the `dream` skill instructions. Scaffold into `games/<slug>/`.
