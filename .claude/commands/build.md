Run build verification: $ARGUMENTS

Read `CLAUDE.md` first. If an argument starts with `games/`, use it as the game directory. Otherwise, auto-detect the game by finding the most recently modified `games/*/GAME_DESIGN.md`. Follow the `build` skill instructions.
