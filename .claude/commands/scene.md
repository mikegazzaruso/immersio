Create or modify a game level: $ARGUMENTS

Read `CLAUDE.md` first. If the first argument starts with `games/`, use it as the game directory. Otherwise, auto-detect the game by finding the most recently modified `games/*/GAME_DESIGN.md`. Follow the `scene` skill instructions.
